const addMenuItems = require('../menu/add-menu-item');
const updateMenuItems = require('../menu/upd-menu-items');
const deleteMenuItems = require('../menu/del-menu-items');

module.exports = {
    addMenuItems,
    updateMenuItems,
    deleteMenuItems,
};