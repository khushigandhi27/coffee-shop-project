const addOrderItems = require('../order/add-order-item');
const updateOrderItems = require('../order/upd-order-item');
const deleteOrderItems = require('../order/del-order-items');

module.exports = {
    addOrderItems,
    updateOrderItems,
    deleteOrderItems,
};